# Level 1

We need to find a file called *readme* in the home directory. That's where our password to level 1 is.

We can connect to bandit0 as we did in level 0. Once we do, we'll be in the home directory, which we can verify by using the *pwd* command. If we run the *ls* command to list all the files here, we see our readme file.

Now we just need to read the file contents. We can do this using *cat* or by opening the file in an editor like nano or vim.

Once we do, we find the password: ZjLjTmM6FvvyRnrb2rfNWOZOTa6ip5If
